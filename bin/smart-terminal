#!/usr/bin/env node
const _ = require('underscore');
const program = require('commander');

program
	.version('1.0.0')
	.description('下挂终端管理');

program
	.command('db <action>','管理下挂终端数据库管理')

program.parse(process.argv);